{
  "name": "music_app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "docker:build": "docker build --tag music-app .",
    "docker:build:nocache": "docker build --no-cache --tag music-app .",
    "docker:start": "docker compose up music-app",
    "docker:build:start": "npm run docker:build && npm run docker:start"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^5.3.0",
    "ejs": "^5.0.2",
    "express": "^5.2.1",
    "googleapis": "^171.4.0",
    "mp3tag.js": "^3.16.0",
    "serve-favicon": "^2.5.1",
    "youtube-dl-exec": "^3.1.7"
  }
}
